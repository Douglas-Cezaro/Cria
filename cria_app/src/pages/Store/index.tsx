import React from "react";
import { Text, View } from "react-native";
import { Container } from "./styles";

export default function Store() {
  return (
    <Container>
      <Text style={{ textAlign: "center", alignItems: "center" }}>Store</Text>
    </Container>
  );
}
